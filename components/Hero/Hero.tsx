import React from 'react';
import styled from 'styled-components';
import { HeroWrapper, Title, Subtitle } from './Hero.styled';

const Hero = () => {
  return (
    <HeroWrapper>
        <Title>Hero</Title>
        <Subtitle>I'm a UI/Accessibility Engineer</Subtitle>
    </HeroWrapper>
  )
}

export default Hero;